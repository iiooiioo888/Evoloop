以下是 **從零開始開發 EvoLoop 的完整任務提示詞（Task Prompts）**，按階段劃分，每個任務都包含明確目標與產出，可直接用於專案管理工具或逐步實作。

---

# EvoLoop 開發任務清單

## Phase 0：環境與基礎建設
### Task 0.1 初始化專案與版本控制
- 建立 GitHub 儲存庫 `evoloop`
- 設定 `.gitignore`（Python、Node、環境變數）
- 建立專案根目錄結構（`backend/`, `frontend/`, `opc_service/`, `dspy_pipeline/`, `docker/`）
- 撰寫 `README.md` 簡述專案目標

### Task 0.2 建立開發環境
- 建立 Python 虛擬環境，安裝基礎依賴（FastAPI, LangChain, LangGraph, LiteLLM）
- 建立 React 前端專案（Vite + TypeScript）
- 設定環境變數管理（`.env` 範例檔，包含 `OPENAI_API_KEY`）
- 建立 `docker-compose.yml` 骨架

### Task 0.3 外部 LLM 連線測試
- 安裝 LiteLLM，撰寫測試腳本呼叫 GPT-4 並取得回覆
- 驗證 API 金鑰、速率限制處理

---

## Phase 1：EvoLoop 核心閉環
### Task 1.1 定義 EvoLoop 狀態模型
- 設計 `EvoLoopState` TypedDict，包含所有必要欄位（查詢、回答、評分、反思、記憶等）

### Task 1.2 設計基礎 Prompt 模板
- 建立 `prompts/` 目錄，撰寫「生成初始回答」、「自動評估」、「反思與建議」、「優化回答」四組 Prompt
- 每組 Prompt 需支援參數化輸入

### Task 1.3 實作 LangGraph 節點
- 建立 `core/nodes.py`，實作以下函式：
  - `generate_initial_answer`
  - `evaluate_answer`
  - `reflect`
  - `improve_answer`
  - `decide_final_answer`
- 每個節點接收並回傳 `EvoLoopState`

### Task 1.4 建構反思迴圈圖
- 建立 `core/graph.py`，組合節點與條件路由
- 定義 `should_improve` 路由邏輯（評分 < 8 → reflect，否則 finalize）
- 加入記憶儲存節點（暫時寫入 JSON 檔案）

### Task 1.5 反思迴圈單元測試
- 使用模擬 LLM 回覆（或真實 API）測試完整流程
- 驗證低分時觸發反思、高分時直接輸出

---

## Phase 2：記憶與向量資料庫
### Task 2.1 建立向量資料庫服務
- 使用 ChromaDB，建立 `memory/` 模組
- 實作 `add_memory(text, metadata)` 與 `search_similar(query, k)` 函式

### Task 2.2 記憶儲存節點重構
- 將反思成功後的經驗（反思文字 + 改進答案）嵌入並存入 Chroma
- 在 `generate_initial_answer` 前加入檢索節點，動態注入相似經驗作為 few-shot 提示

### Task 2.3 記憶管理工具
- 實作定時清理過期或低品質記憶的腳本
- 提供手動重置記憶庫的 API

---

## Phase 3：FastAPI 後端服務
### Task 3.1 建立後端 API 框架
- 使用 FastAPI 建立 `main.py`，掛載 CORS、日誌中介層
- 建立 `/health` 端點

### Task 3.2 聊天與反思端點
- `POST /chat`：接收使用者查詢，執行完整 EvoLoop 圖，回傳最終回答與反思紀錄（可選）
- `POST /feedback`：接收使用者 👍/👎，觸發異步反思任務

### Task 3.3 串流回應支援
- 使用 SSE（Server-Sent Events）實作串流輸出 LLM 最終回答
- 前端對應調整為逐字顯示

### Task 3.4 使用者會話管理
- 引入 Redis，管理 `session_id` 與對應的對話摘要
- 在多輪對話中載入歷史摘要合併至查詢

---

## Phase 4：前端對話介面
### Task 4.1 聊天 UI 元件
- 建立 `ChatWindow` 元件，包含訊息列表、輸入框、傳送按鈕
- 實作訊息角色區分（使用者/AI）與時間戳

### Task 4.2 串流訊息顯示
- 使用 `EventSource` 或 WebSocket 接收 SSE 串流，逐字渲染 AI 回覆
- 加入打字動畫效果

### Task 4.3 用戶回饋按鈕
- 每則 AI 訊息下方加入 👍👎 按鈕
- 點擊後呼叫 `/feedback` API，並顯示感謝提示

### Task 4.4 多輪對話與歷史
- 支援載入歷史對話（透過 session）
- 自動滾動至最新訊息

### Task 4.5 響應式設計與錯誤處理
- 適配手機/平板
- 處理網路中斷、API 錯誤的提示與重試機制

---

## Phase 5：DSPy 自動提示優化
### Task 5.1 收集訓練資料
- 從反思記憶或成功案例中導出 `(query, answer)` 對，準備 DSPy 訓練集
- 格式轉換為 `dspy.Example`

### Task 5.2 定義 DSPy 模組
- 建立 `CustomerServiceResponse` 簽章
- 實作 `AgentModule`（ChainOfThought）

### Task 5.3 設定自動優化流程
- 使用 `BootstrapFewShot` 或 `MIPROv2` 編譯模組
- 撰寫自訂評估指標（可基於 LLM 評分或人工標註）

### Task 5.4 部署優化提示
- 將最佳化的 Prompt 匯出為 JSON，並在 LangGraph 的 `generate_initial_answer` 中動態載入
- 實作 A/B 測試機制（新舊提示分流）

---

## Phase 6：多代理人審查（品質保證）
### Task 6.1 建立代理人角色
- 建立 `PolicyChecker`、`EmpathyReviewer` 節點，各呼叫 LLM 進行專項評估
- 定義各自的評估 Prompt

### Task 6.2 設計融合邏輯
- 建立 `synthesize_final` 節點，整合原始回答與審查意見，生成最終版本
- 在 LangGraph 中加入平行執行（asyncio.gather）

### Task 6.3 安全與成本控制
- 實作風險分級：高風險問題才啟動多代理人，節省 API 成本
- 設定審查超時與降級機制

---

## Phase 7：OPC 整合（工業場景）
### Task 7.1 建立 OPC 微服務
- 實作 `opc_service`（FastAPI），封裝 OPC UA 讀寫
- 提供 `/read`, `/write`, `/tags`, WebSocket 訂閱端點

### Task 7.2 安全護欄實作
- 寫入白名單、數值邊界檢查、審計日誌
- 可選人類審批 API（高風險操作暫停，待人工確認）

### Task 7.3 EvoLoop 整合 OPC 節點
- 新增 `sense_opc`、`act_opc` 節點，連接工業數據流
- 撰寫工業診斷 Prompt（讓 LLM 理解製程參數）

### Task 7.4 模擬環境閉環測試
- 使用模擬 OPC 伺服器，模擬異常情境（溫度超標），驗證系統自動判斷、調整、反思的完整流程

---

## Phase 8：測試、部署與監控
### Task 8.1 單元測試與整合測試
- 為每個節點撰寫單元測試（使用模擬 LLM 回覆）
- 端對端測試：前端 → 後端 → OPC 微服務

### Task 8.2 CI/CD 管線
- 建立 GitHub Actions，自動執行測試、建置 Docker 映像、推送至容器倉庫
- 設定部署至開發/生產環境

### Task 8.3 容器化部署
- 完善 `docker-compose.yml`，包含前端、後端、OPC 服務、Chroma、Redis 等
- 設定環境變數與持久化磁碟區

### Task 8.4 監控與警報
- 整合 Prometheus 指標（API 延遲、LLM 呼叫次數、反思成功率）
- 建立 Grafana 儀表板
- 設定成本追蹤與異常警報

### Task 8.5 安全強化
- API 速率限制、JWT 認證
- 敏感資料加密儲存
- 定期弱點掃描

---

## Phase 9：文件與知識轉移
### Task 9.1 API 文件
- 使用 FastAPI 自動產生 Swagger 文件，並補充說明

### Task 9.2 開發者指南
- 撰寫貢獻指南、程式碼風格規範
- 記錄架構決策（ADR）

### Task 9.3 使用者操作手冊
- 前端功能介紹、管理後台操作說明
- 異常處理與常見問題

---

這份任務提示詞清單涵蓋了從零建置 EvoLoop 的全生命週期。你可以根據團隊資源與優先級，挑選階段逐步執行；每個 Task 提示詞都可直接貼入 AI 助手（如 ChatGPT）中，要求其產生對應的程式碼或配置文件，加速開發進程。


