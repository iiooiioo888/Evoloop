以下是 **EvoLoop 每個任務的詳細實作指引**，涵蓋從零到完整部署的所有步驟，每個任務附帶關鍵程式碼、配置範例與注意事項。你可直接將這些指引作為開發手冊，逐步執行。

---

## Phase 0：環境與基礎建設

### Task 0.1 初始化專案與版本控制
- **目標**：建立標準化專案結構與版本控制。
- **步驟**：
  1. 在 GitHub 建立新儲存庫 `evoloop`。
  2. 本地 `git clone` 後，建立以下目錄結構：
     ```
     evoloop/
     ├── backend/
     │   ├── api/
     │   ├── core/
     │   ├── services/
     │   ├── memory/
     │   └── main.py
     ├── frontend/
     ├── opc_service/
     ├── dspy_pipeline/
     ├── docker/
     ├── tests/
     ├── .env.example
     ├── .gitignore
     ├── README.md
     └── docker-compose.yml
     ```
  3. `.gitignore` 內容加入 `__pycache__`, `.env`, `venv/`, `node_modules/`, `.DS_Store` 等。
  4. `README.md` 初始內容：專案名、簡介、快速開始步驟。
- **注意**：盡早定義目錄約定，確保團隊一致。

### Task 0.2 建立開發環境
- **目標**：建置可立即開發的 Python 與前端環境。
- **步驟**：
  - **後端**：
    1. 在 `backend/` 建立虛擬環境：`python -m venv venv && source venv/bin/activate`（Windows: `venv\Scripts\activate`）。
    2. 建立 `requirements.txt`，內容：
       ```
       fastapi==0.110.0
       uvicorn[standard]==0.27.1
       langgraph>=0.0.20
       langchain>=0.1.0
       langchain-openai>=0.0.5
       liteLLM>=1.0.0
       chromadb>=0.4.0
       pydantic-settings>=2.1.0
       httpx>=0.27.0
       redis>=5.0.0
       celery>=5.3.0
       ```
       安裝：`pip install -r requirements.txt`。
  - **前端**：
    1. 在 `frontend/` 執行 `npm create vite@latest . -- --template react-ts`，依照提示設定。
    2. 安裝依賴：`npm install`。
    3. 安裝 Tailwind CSS：依官方指南初始化。
  - **環境變數**：複製 `.env.example` 為 `.env`，填入 `OPENAI_API_KEY`、`REDIS_URL` 等。
- **注意**：所有依賴版本盡量鎖定，避免後續相容問題。

### Task 0.3 外部 LLM 連線測試
- **目標**：確認能透過 LiteLLM 成功呼叫 OpenAI API。
- **步驟**：
  1. 在 `backend/` 建立 `test_llm.py`：
     ```python
     import litellm
     litellm.api_key = "your-key"
     response = litellm.completion(model="gpt-4o", messages=[{"role": "user", "content": "Hello"}])
     print(response.choices[0].message.content)
     ```
  2. 執行 `python test_llm.py`，觀察是否回傳正常。
- **注意**：若使用其他 LLM，LiteLLM 只需改 `model` 參數（如 `claude-3-opus-20240229`）並設定對應 API key。

---

## Phase 1：EvoLoop 核心閉環

### Task 1.1 定義 EvoLoop 狀態模型
- **目標**：建立所有節點共用的狀態結構。
- **步驟**：在 `backend/core/state.py` 定義：
  ```python
  from typing import TypedDict, List, Optional

  class EvoLoopState(TypedDict):
      user_query: str
      initial_answer: str
      evaluation_score: float
      evaluation_feedback: str
      needs_improvement: bool
      reflection: str
      improved_answer: str
      final_answer: str
      memory_items: List[str]
  ```
- **注意**：可依應用場景擴充（例如加入 `session_id`, `opc_readings` 等）。

### Task 1.2 設計基礎 Prompt 模板
- **目標**：建立可覆用的提示詞。
- **步驟**：在 `backend/prompts/` 下建立四個 `.txt` 或 `.py` 字串變數。
  - `generate_prompt.txt`：參考先前設計，包含 `{user_query}` 佔位。
  - `evaluate_prompt.txt`：要求輸出 JSON `{score, feedback, needs_improvement}`。
  - `reflect_prompt.txt`：輸入原始回答、回饋，輸出改進方針。
  - `improve_prompt.txt`：結合反思，重新生成回答。
- **範例**（generate）：
  ```
  你是一位樂於助人的 AI 助理。請根據使用者問題提供清晰、有用的回答。
  使用者問題：{user_query}
  回答：
  ```
- **注意**：模板可使用 Python `string.Template` 或 f-string 載入。

### Task 1.3 實作 LangGraph 節點
- **目標**：建立每個狀態轉換函式。
- **步驟**：在 `backend/core/nodes.py` 實作。
  ```python
  import json
  from langchain_openai import ChatOpenAI
  from langchain.schema import HumanMessage
  from .state import EvoLoopState
  from .prompts import GENERATE_PROMPT, EVALUATE_PROMPT, REFLECT_PROMPT, IMPROVE_PROMPT

  llm = ChatOpenAI(model="gpt-4o", temperature=0)

  def generate_initial(state: EvoLoopState) -> EvoLoopState:
      prompt = GENERATE_PROMPT.format(user_query=state["user_query"])
      msg = llm.invoke([HumanMessage(content=prompt)])
      state["initial_answer"] = msg.content
      return state

  def evaluate(state: EvoLoopState) -> EvoLoopState:
      answer = state.get("improved_answer") or state["initial_answer"]
      prompt = EVALUATE_PROMPT.format(user_query=state["user_query"], answer=answer)
      msg = llm.invoke([HumanMessage(content=prompt)])
      try:
          res = json.loads(msg.content)
      except:
          res = {"score": 0, "feedback": "eval error", "needs_improvement": True}
      state["evaluation_score"] = res["score"]
      state["evaluation_feedback"] = res["feedback"]
      state["needs_improvement"] = res["needs_improvement"]
      return state

  def reflect(state: EvoLoopState) -> EvoLoopState:
      answer = state.get("improved_answer") or state["initial_answer"]
      prompt = REFLECT_PROMPT.format(user_query=state["user_query"], answer=answer, feedback=state["evaluation_feedback"])
      msg = llm.invoke([HumanMessage(content=prompt)])
      state["reflection"] = msg.content
      state["memory_items"].append(f"Q:{state['user_query'][:50]}... | R:{msg.content}")
      return state

  def improve(state: EvoLoopState) -> EvoLoopState:
      prompt = IMPROVE_PROMPT.format(user_query=state["user_query"], reflection=state["reflection"])
      msg = llm.invoke([HumanMessage(content=prompt)])
      state["improved_answer"] = msg.content
      return state

  def finalize(state: EvoLoopState) -> EvoLoopState:
      state["final_answer"] = state.get("improved_answer") or state["initial_answer"]
      return state
  ```
- **注意**：若使用非同步，可定義 `async def`。

### Task 1.4 建構反思迴圈圖
- **目標**：使用 LangGraph 串聯節點。
- **步驟**：`backend/core/graph.py`
  ```python
  from langgraph.graph import StateGraph, END
  from .state import EvoLoopState
  from .nodes import generate_initial, evaluate, reflect, improve, finalize

  def should_improve(state: EvoLoopState) -> str:
      return "reflect" if state["needs_improvement"] else "finalize"

  workflow = StateGraph(EvoLoopState)
  workflow.add_node("generate", generate_initial)
  workflow.add_node("evaluate", evaluate)
  workflow.add_node("reflect", reflect)
  workflow.add_node("improve", improve)
  workflow.add_node("finalize", finalize)

  workflow.set_entry_point("generate")
  workflow.add_edge("generate", "evaluate")
  workflow.add_conditional_edges("evaluate", should_improve, {"reflect": "reflect", "finalize": "finalize"})
  workflow.add_edge("reflect", "improve")
  workflow.add_edge("improve", "evaluate")  # 重新評估
  workflow.add_edge("finalize", END)

  app = workflow.compile()
  ```
- **注意**：可先以 `app.invoke(initial_state)` 測試。

### Task 1.5 反思迴圈單元測試
- **目標**：驗證流程正確性。
- **步驟**：使用 `pytest` 撰寫測試，模擬 LLM 回應。
  - 安裝 `pytest`, `responses` 或直接手動 mock `llm.invoke`。
  - 測試情境：
    1. 評分 9 分 → 直接 finalize，無反思。
    2. 評分 5 分 → 進入 reflect → improve → 再評估。
- **注意**：避免依賴實際 API，加快測試速度。

---

## Phase 2：記憶與向量資料庫

### Task 2.1 建立向量資料庫服務
- **目標**：使用 ChromaDB 管理經驗記憶。
- **步驟**：
  1. 在 `backend/memory/vector_store.py` 建立包裝：
     ```python
     from chromadb import Client
     from chromadb.config import Settings
     from langchain_openai import OpenAIEmbeddings

     embeddings = OpenAIEmbeddings()
     client = Client(Settings(persist_directory="./chroma_db"))
     collection = client.get_or_create_collection("evo_memory")

     def add_memory(text: str, metadata: dict, id: str = None):
         collection.add(documents=[text], metadatas=[metadata], ids=[id or str(uuid.uuid4())])

     def search_memory(query: str, k=3):
         results = collection.query(query_texts=[query], n_results=k)
         return results
     ```
- **注意**：生產環境可改用 Pinecone 等雲端服務。

### Task 2.2 記憶儲存節點重構
- **目標**：將反思結果存入 Chroma，並於生成時檢索。
- **步驟**：
  - 在 `nodes.py` 加入 `save_memory` 節點，調用 `add_memory`。
  - 加入 `retrieve_memory` 節點，根據 `user_query` 搜尋相關記憶，拼接到 Prompt 的「過往經驗」段落。
  - 修改 `generate_initial` 讓其接受 `context` 參數。
  - 在圖中加入 retrieve → generate 的順序。
- **注意**：避免上下文過長，限制檢索數量。

### Task 2.3 記憶管理工具
- **目標**：提供維護 API。
- **步驟**：
  - 實作定時清理腳本（如刪除 30 天前的低分記憶）。
  - 在 FastAPI 中新增 `DELETE /memory/reset` 端點（需管理員權限）。
- **注意**：謹慎設計重置功能，避免誤刪重要經驗。

---

## Phase 3：FastAPI 後端服務

### Task 3.1 建立後端 API 框架
- **目標**：提供基礎 HTTP 服務。
- **步驟**：
  - `backend/main.py`:
    ```python
    from fastapi import FastAPI
    from fastapi.middleware.cors import CORSMiddleware
    app = FastAPI(title="EvoLoop API")
    app.add_middleware(CORSMiddleware, allow_origins=["*"], allow_methods=["*"], allow_headers=["*"])
    @app.get("/health")
    def health(): return {"status": "ok"}
    ```
  - 啟動：`uvicorn main:app --reload`。
- **注意**：CORS 在生產環境應限制為前端網域。

### Task 3.2 聊天與反思端點
- **目標**：建立 `/chat` 端點執行 EvoLoop 圖。
- **步驟**：
  - `backend/api/chat.py`:
    ```python
    from fastapi import APIRouter
    from core.graph import app
    from core.state import EvoLoopState
    router = APIRouter()

    @router.post("/chat")
    async def chat(query: str):
        state = EvoLoopState(
            user_query=query,
            initial_answer="", evaluation_score=0.0, evaluation_feedback="",
            needs_improvement=False, reflection="", improved_answer="", final_answer="",
            memory_items=[]
        )
        result = app.invoke(state)
        return {"answer": result["final_answer"], "reflection": result.get("reflection")}
    ```
  - 在 `main.py` 中掛載路由。
- **注意**：異步版本需使用 `app.ainvoke`。

### Task 3.3 串流回應支援
- **目標**：透過 SSE 推送生成文字。
- **步驟**：
  1. 將 `finalize` 節點的回應改為生成器，或使用 LangChain 的 `StreamingStdOutCallbackHandler`。
  2. FastAPI 端點：
     ```python
     from fastapi.responses import StreamingResponse
     async def stream_answer(query: str):
         async def event_stream():
             # 模擬逐字產出
             for token in final_answer:
                 yield f"data: {token}\n\n"
         return StreamingResponse(event_stream(), media_type="text/event-stream")
     ```
- **注意**：若 LLM 本身支援串流，直接轉發 token。

### Task 3.4 使用者會話管理
- **目標**：支援多輪對話。
- **步驟**：
  1. 使用 Redis 儲存每個 session 的對話歷史摘要。
  2. 建立 `get_session_history` 與 `save_session_history` 工具函式。
  3. 在 `/chat` 端點接收 `session_id`，載入歷史摘要併入 `user_query`。
- **注意**：摘要可使用 `ConversationSummaryMemory` 來自動生成。

---

## Phase 4：前端對話介面

### Task 4.1 聊天 UI 元件
- **目標**：基本聊天介面。
- **步驟**：使用 React 建立 `ChatWindow`，內部包含：
  - `MessageList`：渲染訊息陣列。
  - `InputBar`：文字輸入與發送按鈕。
  - 狀態管理：`useState` 儲存訊息列表。
- **範例程式碼片段**：
  ```tsx
  const [messages, setMessages] = useState<{role:string, content:string}[]>([]);
  const sendMessage = async (text: string) => {
      setMessages(prev => [...prev, {role:'user', content:text}]);
      // call API
  };
  ```
- **注意**：使用 Tailwind 快速美化。

### Task 4.2 串流訊息顯示
- **目標**：即時顯示 AI 回覆。
- **步驟**：
  1. 前端使用 `EventSource` 連接 `/chat/stream`。
  2. 接受到資料後，動態更新最後一條 AI 訊息。
- **注意**：處理連線中斷重連。

### Task 4.3 用戶回饋按鈕
- **目標**：收集 👍/👎。
- **步驟**：每條 AI 訊息下方加入兩個圖示按鈕，點擊後 POST `/feedback` 攜帶訊息 ID 與評分。

### Task 4.4 多輪對話與歷史
- **目標**：支援頁面重整後恢復對話。
- **步驟**：前端根據 `session_id` 請求 `/history` API，取得歷史訊息並渲染。

### Task 4.5 響應式設計與錯誤處理
- **步驟**：
  - 使用 Tailwind 斷點調整佈局。
  - 加入 `try-catch` 與全域錯誤邊界 (Error Boundary)。
  - 網路中斷時顯示重試按鈕。

---

## Phase 5：DSPy 自動提示優化

### Task 5.1 收集訓練資料
- **目標**：從反思記錄匯出高品質資料。
- **步驟**：
  1. 從 Chroma 或日誌中提取 `user_query`、對應的 `improved_answer`（若存在）。
  2. 過濾掉評分仍低於 8 的案例。
  3. 轉換為 `dspy.Example` 格式。

### Task 5.2 定義 DSPy 模組
- **步驟**：`dspy_pipeline/module.py`
  ```python
  import dspy
  class ResponseSignature(dspy.Signature):
      context = dspy.InputField()
      question = dspy.InputField()
      answer = dspy.OutputField(desc="helpful, accurate response")

  class EvoLoopModule(dspy.Module):
      def __init__(self):
          super().__init__()
          self.gen = dspy.ChainOfThought(ResponseSignature)
      def forward(self, question, context=""):
          return self.gen(context=context, question=question)
  ```

### Task 5.3 設定自動優化流程
- **步驟**：
  1. 載入訓練資料為 `trainset`。
  2. 使用 `BootstrapFewShot`：
     ```python
     from dspy.teleprompt import BootstrapFewShot
     optimizer = BootstrapFewShot(metric=my_metric)
     optimized = optimizer.compile(EvoLoopModule(), trainset=trainset)
     ```
  3. `my_metric` 可呼叫 LLM 比較回答相似度或人工標註。
- **注意**：初次執行可使用簡單規則（如長度、關鍵字包含）。

### Task 5.4 部署優化提示
- **步驟**：
  1. 匯出模組的提示：`optimized.save("optimized_prompt.json")`。
  2. 修改 `generate_initial` 節點，讀取此 JSON 並動態組裝 Prompt。
  3. 實現 A/B 測試：根據分流標誌使用新舊提示。
- **注意**：保留所有歷史版本。

---

## Phase 6：多代理人審查（品質保證）

### Task 6.1 建立代理人角色
- **步驟**：
  - 建立 `policy_checker` 節點：呼叫 LLM 檢查法規/政策符合度。
  - 建立 `empathy_reviewer`：評估語氣與同理心。
  - 各自的 Prompt 類似評估，但針對特定面向。
- **程式碼範例**：
  ```python
  def policy_check(state):
      prompt = f"檢查以下回答是否符合公司政策...\n{state['initial_answer']}"
      msg = llm.invoke([HumanMessage(content=prompt)])
      state["policy_feedback"] = msg.content
      return state
  ```

### Task 6.2 設計融合邏輯
- **步驟**：
  - 建立 `synthesize` 節點，接收所有審查回饋，生成最終回答。
  - 在圖中平行執行審查節點（可使用 `asyncio.gather`）。
- **注意**：平行執行需注意狀態合併的執行緒安全。

### Task 6.3 安全與成本控制
- **步驟**：
  - 定義高風險關鍵字（如醫療建議），觸發多代理人。
  - 否則跳過審查，節省 API 呼叫。
  - 實作超時機制。

---

## Phase 7：OPC 整合（工業場景）

### Task 7.1 建立 OPC 微服務
- **步驟**：已提供完整 FastAPI 程式碼骨架（參考先前回覆）。
- **注意**：確保 OPC 伺服器可連線，並處理斷線重連。

### Task 7.2 安全護欄實作
- **步驟**：
  - 在 `opc_service` 中實作白名單檢查（`settings.tags_whitelist`）。
  - 寫入數值邊界驗證（`settings.write_bounds`）。
  - 記錄所有寫入操作至日誌（使用 logging 或資料庫）。
- **注意**：可加入 JWT 驗證保護寫入端點。

### Task 7.3 EvoLoop 整合 OPC 節點
- **步驟**：
  - 新增 `sense_opc`：透過 HTTP 呼叫 OPC 服務讀取標籤。
  - 新增 `act_opc`：將 EvoLoop 輸出的控制動作寫入 OPC。
  - 在 LangGraph 中加入這些節點，於適當位置連接。
- **範例**：
  ```python
  async def sense_opc(state):
      async with httpx.AsyncClient() as client:
          resp = await client.post("http://opc-service:8001/opc/read", json={"tag_name": "Temperature"})
          state["temperature"] = resp.json()["value"]
      return state
  ```

### Task 7.4 模擬環境閉環測試
- **步驟**：
  1. 使用 PysdnOPC 提供的模擬伺服器或 Prosys OPC UA Simulation Server。
  2. 設定一個異常情境（如溫度逐步升高超過閾值）。
  3. 執行 EvoLoop，觀察是否觸發異常判斷、產生控制建議、寫入冷卻閥、並將成功案例存入記憶。
- **注意**：先確保安全護欄正常運作，避免真實設備受影響。

---

## Phase 8：測試、部署與監控

### Task 8.1 單元測試與整合測試
- **步驟**：
  - 為每個節點撰寫獨立測試，mock LLM 輸出。
  - 使用 `pytest-asyncio` 測試非同步端點。
  - 整合測試：啟動測試容器，用 HTTP 客戶端發送 `/chat` 請求驗證完整流程。

### Task 8.2 CI/CD 管線
- **步驟**：在 GitHub 建立 `.github/workflows/ci.yml`：
  ```yaml
  name: CI
  on: [push]
  jobs:
    test:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: Set up Python
          uses: actions/setup-python@v4
          with:
            python-version: '3.11'
        - run: pip install -r backend/requirements.txt
        - run: pytest
    build:
      runs-on: ubuntu-latest
      needs: test
      steps:
        - uses: actions/checkout@v3
        - name: Build and push Docker images
          # 使用 docker/build-push-action
  ```
- **注意**：加入前端建置與測試。

### Task 8.3 容器化部署
- **步驟**：完善 `docker-compose.yml`，加入所有服務：
  ```yaml
  services:
    backend:
      build: ./backend
      ports: ["8000:8000"]
      environment: ...
      depends_on: [redis, chroma]
    frontend:
      build: ./frontend
      ports: ["3000:80"]
    opc-service:
      build: ./opc_service
      ports: ["8001:8001"]
    redis:
      image: redis:alpine
    chroma:
      image: chromadb/chroma
    celery-worker:
      build: ./backend
      command: celery -A tasks worker --loglevel=info
  ```
- **注意**：設定 volume 持久化資料。

### Task 8.4 監控與警報
- **步驟**：
  1. 在後端加入 Prometheus 指標端點（`prometheus_fastapi_instrumentator`）。
  2. 設定 Grafana 儀表板，監控請求延遲、錯誤率、LLM 花費。
  3. 設定警報：成本超標、反思成功率下降。
- **注意**：成本追蹤可整合 LiteLLM 的回呼。

### Task 8.5 安全強化
- **步驟**：
  - 加入速率限制（`slowapi`）。
  - 管理 API 使用 JWT 保護。
  - 敏感資料（API key）使用 Docker secrets 或環境變數加密。
- **注意**：定期進行依賴漏洞掃描（`pip-audit`）。

---

## Phase 9：文件與知識轉移

### Task 9.1 API 文件
- **步驟**：FastAPI 自動產生 `/docs`，確保所有路由有 `summary` 與 `description`。

### Task 9.2 開發者指南
- **步驟**：
  - 撰寫 `CONTRIBUTING.md`：如何設定環境、提交程式碼、執行測試。
  - 記錄架構決策 (ADR) 於 `docs/architecture/` 中。

### Task 9.3 使用者操作手冊
- **步驟**：
  - 說明前端功能、後台管理（若有）。
  - 提供常見問題排解。

---
